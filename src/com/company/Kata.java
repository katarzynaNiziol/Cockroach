package com.company;

import java.util.Arrays;
import java.util.Collections;

public class Kata {
    public int min(int[] list) {
//
        Integer[] lists = {};
        int min = (int) Collections.min(Arrays.asList(lists));
        System.out.println(min);
    }

    public int max(int[] list) {
        Integer[] lists = {};
        int max = (int) Collections.max(Arrays.asList(lists));
        System.out.println(max);
    }
            return 0;
}